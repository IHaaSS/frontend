import { EventConstraintPipe } from './event-constraint.pipe';

describe('EventConstraintPipe', () => {
  it('create an instance', () => {
    const pipe = new EventConstraintPipe();
    expect(pipe).toBeTruthy();
  });
});
